/**
 * Team id for Disguised (DSG). Members of this team get the "disguised"
 * brand theme (see `.disguised` in globals.css) and default into it.
 */
export const DSG_TEAM_ID = 345;

/** Disguised brand primary color, used by the `.disguised` theme. */
export const DSG_BRAND_PRIMARY = "#F7B150";
