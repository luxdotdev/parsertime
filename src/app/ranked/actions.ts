"use server";

// Stub — full implementation in Task 6
export function deleteRankedMatch(
  _matchId: string
): Promise<{ success: boolean; error?: string }> {
  return Promise.reject(new Error("Not implemented"));
}
